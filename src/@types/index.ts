export type ScanDataTypes = {
  ipAddress: string;
  macAddress: string;
  ipType: string;
  streamPort: number;
  httpPort: number;
  upgradePort: number;
  serverName: string;
  model: string;
  firmwareVersion: string;
  resolutions: string;
  videoFormat: string;
};
